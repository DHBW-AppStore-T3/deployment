### 2026-09-15 — CORS_ORIGINS Fix gemerged, Remotes auf T3 korrigiert
Lokale Remotes zeigten auf die falsche, gleichnamige Org
(DHBW-AppStore statt DHBW-AppStore-T3). Divergenz gemerged (1 Commit
von T3, 30 von hier), auf T3 gepusht, alle vier Remotes umgebogen.
Stand danach: alle Repos zeigen korrekt auf DHBW-AppStore-T3.

### 2026-09-15 — Branch-Protection org-weit gesetzt
PR-Pflicht, Required-Status-Checks (wo sinnvoll), kein Force-Push in
allen sechs Repos. Dabei entdeckt: GitHub Actions hatte in der
gesamten Org nie einen Workflow-Run ausgeführt (Fork-Repos brauchen
eine manuelle "Enable workflows"-Bestätigung). Nach Freischaltung
zum ersten Mal echte CI-Läufe — Security-Checks fanden sofort echte,
unpatched CVEs in backend/frontend/worker.

### 2026-09-15 — Hermes Agent + podman-mcp live auf appstore-prod-01
Fünf reale Bugs beim ersten Deploy gefunden und behoben (kein
veröffentlichtes podman-mcp-Image, falsche Backend-Auswahl,
blockierender stdin, fehlender Healthcheck, kein automatischer
MCP-Reconnect) — siehe `.github`-Repo `HARNESS.md` Abschnitt 2.1.
Discord-Integration als bewusste Revision von "SSH-only" (System 3.2)
hinzugefügt, getestet, funktioniert. Ein separater
`claude-agent`-Host-User wurde angelegt, dann wieder verworfen —
Hermes selbst ist der eine Server-Agent.

### 2026-09-15/16 — PreToolUse-Guardrail, claude_docs/ + Graphify org-weit
`.claude/hooks/appstore-prod-guardrail.py` schließt die letzte offene
Lücke aus System 3.2 (direkter SSH-Zugriff ohne Guardrail). Parallel:
`claude_docs/` (tief geschachtelt bei backend/frontend/worker/deployment,
flach bei moodle_appstore/self-service-ui) und Graphify-Läufe für alle
Repos außer den beiden Referenz-Repos, als Teil der vollständigen
HARNESS.md-System-1-Umsetzung.
